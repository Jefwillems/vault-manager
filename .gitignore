.idea/
.env

# Copilot CLI bundle artifacts generated by `go tool bundler` at build time.
# These are large, platform-specific, and reproducible; never commit them.
cmd/vault-manager/zcopilot_*.go
cmd/vault-manager/z*.zst
